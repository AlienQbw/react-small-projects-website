import React from 'react';

const History = () => {
  return (
    <div className="productivity-module container">
      <p>History component</p>
      {/*
      Everytime Someone starts the clock and then stops it, it will add session to the history, at the bottom will be a sum
      */}
    </div>
  );
};

export default History;
